# Frontend settings for `npm run dev`. Copy to .env: cp example-dev.env .env
# Vite only exposes variables prefixed with VITE_, and inlines them at build time.
VITE_BACKEND_API_URL=http://backend.localhost
